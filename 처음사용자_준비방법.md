# GHJ Codex V.03 처음 사용자 준비 방법

이 프로그램은 실행 후 웹 화면에서 KRX 아이디와 KRX 비밀번호를 입력하면 데이터 수집이 시작됩니다.

## 1. KRX 아이디/비밀번호 준비

1. KRX 정보데이터시스템에 접속합니다.
   - https://data.krx.co.kr/
2. 회원가입을 진행합니다.
3. 가입한 KRX 아이디와 비밀번호를 준비합니다.
4. 프로그램 웹 화면의 `KRX 아이디`, `KRX 비밀번호` 칸에 입력합니다.

## 2. 인증 방식

별도의 OpenAPI 키는 필요하지 않습니다. 프로그램은 입력한 KRX 계정으로 로그인 세션을 만든 뒤 정보데이터시스템의 JSON 조회 요청을 사용합니다.

비밀번호는 조회 요청 동안만 메모리에서 사용하고 코드나 생성 Excel에 저장하지 않습니다.

## 3. 프로그램 실행

PowerShell에서 아래 순서대로 실행합니다.

```powershell
cd "C:\Users\MK-Desktop\Documents\외국인,기관수급현황실시간대시보드 제작하기"
python ghj_codex_V_03.py
```

브라우저가 자동으로 열리며, 열리지 않으면 아래 주소로 접속합니다.

```text
http://127.0.0.1:5000
```

## 4. 결과 파일 위치

수집이 완료되면 웹 화면에서 `엑셀 다운로드` 버튼을 누를 수 있습니다.

파일은 아래 폴더에도 자동 저장됩니다.

```text
outputs\YYYYMMDD\YYYY-MM-DD_통합파일.xlsx
```

## 참고

KRX 사이트의 로그인 방식이나 내부 조회 형식이 개편되면 프로그램의 수집 모듈도 수정이 필요할 수 있습니다.
